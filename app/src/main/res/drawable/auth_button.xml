<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle"
    android:thickness="@dimen/larger_text_size">
    <padding
        android:left="@dimen/larger_text_size"
        android:bottom="@dimen/larger_text_size"
        android:right="@dimen/larger_text_size"
        android:top="@dimen/larger_text_size"/>
    <corners
        android:radius="10dp" />
    <gradient
        android:angle="270"
        android:startColor="@color/button"
        android:centerColor="@color/button"
        android:endColor="@color/button">
    </gradient>
</shape>